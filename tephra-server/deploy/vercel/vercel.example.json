{
  "$comment": "EXAMPLE ONLY — reference scaffold, not yet deployable. When the Vercel function adapter exists, copy the effective fields into vercel.json at the actual Vercel project root (Root Directory constraints mean deploy/vercel/ itself can never be the project root). Schema: https://vercel.com/docs/project-configuration/vercel-json",
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build --workspace=@tephra/web",
  "installCommand": "npm ci",
  "outputDirectory": "apps/web/dist",
  "regions": ["iad1"],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "regions": ["iad1"]
    }
  },
  "rewrites": [
    { "source": "/healthz", "destination": "/api" },
    { "source": "/readyz", "destination": "/api" },
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/|healthz$|readyz$).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
