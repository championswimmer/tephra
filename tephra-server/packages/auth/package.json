{
  "name": "@tephra/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node -e 'require(\"node:fs\").rmSync(\"dist\",{recursive:true,force:true})' && esbuild src/index.ts --platform=node --format=esm --target=es2022 --bundle --packages=external --outfile=dist/index.js --log-level=error && tsup src/index.ts --dts-only --out-dir dist --silent",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tephra/core": "0.1.0",
    "@tephra/vault-model": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.2",
    "tsup": "^8.5.1"
  }
}
