{
  "name": "@tephra/plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --target=es2022 --outfile=main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tephra/protocol": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "obsidian": "^1.8.7"
  }
}
